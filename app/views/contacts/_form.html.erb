<div class="container">
  <div class="row">
    <%= form_for(@contact) do |f| %>
    <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
        <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <div class="col-md-4 col-lg-4">
      <%= f.text_field :name, :class => "ficatransparente colocaborda largura", placeholder: "NOME", :size => 50 %>
    </div>
    <br>
    <br>
    <div class="col-md-4 col-lg-4">
      <%= f.text_field :email, :class => "ficatransparente colocaborda largura", placeholder: "EMAIL", :size => 50 %>
    </div>
    <br>
    <br>
    <div class="col-md-4 col-lg-4">
      <%= f.text_area :message, :class => "ficatransparente colocaborda textarea", placeholder: "MENSAGEM", :rows => 9%>
    </div>
    <br>
    <br>
    
    <div class="col-md-12">
      <br>
      <%= f.submit :class => "btn btn-primary"%>
    </div>
    <% end %>
  </div>
</div>