
<div class="container">
  <div class="row clearfix">
    <div class="col-md-12 column">
      <div class="row">
       <% @galleries.each do |gallery| %>
        <div class="col-md-4">
          <div class="thumbnail">
         
           <%= link_to ((image_tag (gallery.photos.first.image_url ))), gallery, alt: "300x200" %>
           
            <div class="caption">
              <h3>
                 <td><%= gallery.name %></td>
              </h3>
              <%= markdown(gallery.content) %>
              <p>
                <%= link_to 'Show', gallery, class: "btn btn-primary", role: :button %></a> <a class="btn" href="#">Photos: <%= gallery.photos.size %></a>
              </p>
            </div>
          </div>
        </div>
            <% end %>

      </div>
    </div>
  </div>
</div>
    
     

     

