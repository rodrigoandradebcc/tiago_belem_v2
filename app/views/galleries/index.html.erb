
<div class="container">
  <div class="row clearfix">
    <div class="col-md-12 column">
      <div class="row">
       <% @galleries.each do |gallery| %>
        <div class="col-md-4 col-sm-6">
          <div class="thumbnail">
         
           <%= link_to ((image_tag (gallery.photos.first.image_url ))), gallery %>
           
            <div class="caption" style="word-break: break-all">
              <h3>
                 <td><%= gallery.name %></td>
              </h3>
              <%= markdown(gallery.content) %>
              <p>
                <%= link_to 'Show', gallery, class: "branco btn btn-primary", role: :button %></a> Photos: <%= gallery.photos.size %>
              </p>
            </div>
          </div>
        </div>
            <% end %>

      </div>
    </div>
  </div>
</div>
    
     

     

